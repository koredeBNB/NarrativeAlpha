# CoinMarketCap API key (Pro/Agent Hub). Get one at https://pro.coinmarketcap.com
# Used for both the CMC MCP server (X-CMC-MCP-API-KEY) and the REST fallback (X-CMC_PRO_API_KEY).
CMC_API_KEY=your_cmc_api_key_here
